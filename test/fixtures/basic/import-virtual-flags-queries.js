// Both queries on a named instance, the second of two configured
import flagsRaw from 'virtual:spritemap/__flags?raw'
import flagsUrl from 'virtual:spritemap/__flags?url'

export const href = `${flagsUrl}#sprite-CH`
export const markup = flagsRaw
